{% extends 'base.html.twig' %}
{% block title %}Les Pégis
{% endblock %}

{% block body %}
	<div class="col-md-10 contenu-blanc">
		<h1>Les
			<span>{{ lesPegis | length }}</span>
			pégis  jeux</h1>
		{{ include('messages.html.twig') }}
		<div class="contenu">
			<table class="table table-striped table-advance table-hover">
				<thead>
					<tr class="bg-entete">
						<th>Identifiant</th>
						<th>Pégi</th>
						<th>Description</th>
						<th>Actions</th>
					</tr>
				</thead>
				<tbody>

					<!-- formulaire pour ajouter une nouvelle catégorie-->
					<tr>
						{{ form_start(formCreation) }}
						<td class="col-md-1">Nouveau</td>
						<td class="col-md-6">{{ form_widget(formCreation.ageLimite) }}
							{{ form_errors(formCreation.ageLimite) }}</td>
						<td class="col-md-6">{{ form_widget(formCreation.description) }}
							{{ form_errors(formCreation.description) }}</td>
						<td class="col-md-3">
							<button class="btn btn-primary btn-sm" type="submit" formaction="{{ path('app_pegis_ajouter') }}" title="Enregistrer nouveau pegi">
								<i class="fa fasave"></i>
								Enregistrer</button>
							<button class="btn btn-info btn-sm" type="reset" title="Effacer la saisie">
								<i class="fa fa-eraser"></i>
								Annuler
							</button>
						</td>
						{# Ceci va générer le champ CSRF #}
						{{ form_rest(formCreation) }}
						{{ form_end(formCreation) }}
					</tr>


					{% for key, pegi in lesPegis %}
						<tr>
							{% if pegi.id != idPegiModif %}

								<td>{{ pegi.id }}</td>
								<td>{{ pegi.ageLimite }}</td>
								<td>{{ pegi.description }}</td>
								<!-- formulaire pour demander la modification ou la suppression -->
								<td>
									<form>
										<button type="submit" class="btn btn-primary btn-sm" formaction="{{ path('app_pegis_demandermodification', {'id' : pegi.id }) }}" title="Modifier">
											<i class="fa fa-pencil"></i>Modifier</button>
										<button class="btn btn-danger btn-sm" type="submit" formaction="{{path('app_pegis_supprimer', {'id' : pegi.id }) }}" title="Supprimer" onclick="return confirm('Voulez-vous vraiment supprimer ce pegi ?');">
											<i class="fa fa-trash-o "></i>Supprimer</button>
										<input
										type="hidden" name="_token" value="{{ csrf_token('action-item' ~ pegi.id) }}">
									{# ~ pour concaténer des chaines en twig #}
									</form>
								</td>

							{% else %}
								<!-- formulaire pour modifier un pegi -->
								{{ form_start(formModification) }}
								<td>{{ pegi.id }}</td>
								<td>{{ form_widget(formModification.ageLimite) }}
									{{form_errors(formModification.ageLimite) }}</td>
								<td>{{ form_widget(formModification.description) }}
									{{form_errors(formModification.description) }}</td>
								<td class="col-md-4">
									<button class="btn btn-primary btn-sm" type="submit" formaction="{{path('app_pegis_modifier', {'id' : pegi.id }) }}" title="Enregistrer">
										<i class="fa fa-save"></i>Enregistrer</button>
									<button class="btn btn-info btn-sm" type="reset" title="Effacer la saisie">
										<i class="fa fa-eraser"></i>Effacer</button>
									<button class="btn btn-warning btn-sm" type="submit" formaction="{{ path('app_pegis') }}" title="Annuler">
										<i class="fa fa-undo"></i>Annuler</button>
								</td>
								{# Ceci va générer le champ CSRF #}
								{{ form_rest(formModification) }}
								{{ form_end(formModification) }}
							{% endif %}


						</tr>
					{% endfor %}
				</tbody>
			</table>
		</div>
		<!--fin div contenu-->
	</div>
	<!--fin div col-md-6-->
{% endblock %}
